import React, {useState, useEffect, useContext} from "react";

// Internal Import
import { ChatAppContext } from "../Context/ChatAppContext";

const ChatApp = () => {
  const {  } = useContext(ChatAppContext);
  return <div>Hey, hi</div>;
};

export default ChatApp;